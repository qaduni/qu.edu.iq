{
  "name": "qu.edu.iq",
  "version": "1.0.0",
  "description": "Al-Qadisiyah University Website.",
  "scripts": {
    "build:hugo": "hugo --gc --minify",
    "build:search": "pagefind --site public",
    "build": "npm run build:hugo && npm run build:search",
    "dev": "npm run build && hugo server -D"
  },
  "devDependencies": {
    "pagefind": "^1.5.2"
  }
}